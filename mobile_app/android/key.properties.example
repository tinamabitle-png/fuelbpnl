# Copy to `android/key.properties` and fill in your real values.
# Never commit the real `key.properties` or the keystore file.
storeFile=../bwiser-upload.jks
storePassword=CHANGE_ME
keyPassword=CHANGE_ME
keyAlias=bwiser

